<VirtualHost *:80>
    ServerName localhost

    ProxyPreserveHost On
    ProxyPass / http://dashboard:80/
    ProxyPassReverse / http://dashboard:80/

    ErrorLog ${APACHE_LOG_DIR}/error.log
    CustomLog ${APACHE_LOG_DIR}/access.log combined
</VirtualHost>